def DEFAULT_CPUS   = '28'
def DEFAULT_MEMORY = '60 GB'

process {

  withName: 'TRIMGALORE' {
    cpus = DEFAULT_CPUS
    memory = DEFAULT_MEMORY
  }

  withName: 'STAR_ALIGN_IGENOMES' {
    cpus = DEFAULT_CPUS
    memory = DEFAULT_MEMORY
  }

errorStrategy = 'ignore'

}
